spring:
  application:
    name: iot-verify

  datasource:
    url: jdbc:mysql://localhost:3306/iot_verify?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8&allowPublicKeyRetrieval=true
    username: root
    password: sharkdingo123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

volcengine:
  ark:
    api-key: 8f3151df-403e-4f3d-b559-b35a7618393b
    model-id: ep-20251125202752-bhwbw   # doubao-seed-1-6-251015 对应的 Endpoint ID
    base-url: https://ark.cn-beijing.volces.com/api/v3

