package cn.edu.nju.Iot_Verify.dto.fuzz;

/** Selects the initial-state and search semantics without changing the formal-verification boundary. */
public enum FuzzExplorationMode {
    BOARD_SNAPSHOT,
    PAPER_COMPATIBLE
}
